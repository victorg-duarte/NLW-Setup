// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator erd {
  provider = "prisma-erd-generator"
}

model Habit {
  id         String   @id @default(uuid())
  title      String
  created_at DateTime

  // prisma entender que existe um relacionamento com essa tabela
  dayHabits DayHabit[]
  weekDays  HabitWeekDays[]

  @@map("habits") // nome da tabela
}

model HabitWeekDays {
  id       String @id @default(uuid())
  habit_id String
  week_day Int

  // FK
  habit Habit @relation(fields: [habit_id], references: [id])

  @@unique([habit_id, week_day])
  @@map("habit_week_days")
}

model Day {
  id   String   @id @default(uuid())
  date DateTime // 2023-01-10T00:00:00.000Z

  // prisma entender que existe um relacionamento com essa tabela
  dayHabits DayHabit[]

  @@unique([date])
  @@map("days")
}

model DayHabit {
  id String @id @default(uuid())

  day_id   String
  habit_id String

  // FK
  day   Day   @relation(fields: [day_id], references: [id])
  habit Habit @relation(fields: [habit_id], references: [id])

  @@unique([day_id, habit_id])
  @@map("day_habits")
}
